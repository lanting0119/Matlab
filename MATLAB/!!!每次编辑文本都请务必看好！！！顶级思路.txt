利用matlab原本矩阵计算的特点以及矩阵输出结果的特点（意味着可以用x(i)访问）

将定义变量用新矩阵X=[x1 x2 x3……]表示；方便索引


一开始编程的时候，思考的角度应该是，我最后输出结果要用到什么函数（其输出结果能满足我的要求）→函数输入参数有什么，我如何通过多端函数，从原始数据出发，得到输入参数（有点像证明数学，两边都开始推导）

开发文本，一定要将各个步骤的参数单独算出来，先别急着合并！！！

用好矩阵的连接功能！！！

 filename=['D:\fire_',num2str(i),'.bmp'];（结合循环就能批量生产了）


用好网络资源！！！ 视频 论坛等等


用好覆盖的逻辑！！（即每次覆盖后都输出一次图像，这样相当于就能得到全部图像了！）


电脑和程序能做到很无脑重复去算；所以学会化整为零；暂时先不要想着十分简单的代码解决一切或一个代码能同时处理多种数据→分布成让电脑批量分类，分批处理，处理后重新整合或者单独处理即可；（即不要想着一个循环语句解决高通量，其实可以引入很多个循环语句）


用好每个模块（工程师不一定要非常非常清楚每个砖块的结构和制造工艺（当然知道更好)


高通量不代表着一定要创建中间变量想办法把所有变量汇到一起；而是当能对单个完成，构建循环结构，想办法将变量f的命名与循环体i构建联系方便提取；

单需要同时相同方式处理一批样本数据（如去头去尾）才考虑构建循环变量名（元胞）储存，分组处理（说不定连这个都是多余的）

已知x、y其中一个值如何返还另一个值？（串联两个矩阵，读出一个值索引返还另一个即可）

在计算过程中，需要人为输入的变量都单独赋予名称，这样就不需要在过程中修改

确定计算过程中什么需要保留用于下一步计算（如果不需要被后续重新索引那么编辑就快很多；同时编辑过程中可以留多一个变量储存作为备份）


实验数据未处理可以先做一次图；然后通过图去掉一部分无用信息（这个要手动）